# Instructions for Claude working in this repo

This is a hackathon submission repo (First Commit, Bharat Builds Tour x AWS).
The user is the sole author and sole contributor on record. Follow these
rules for every commit and PR made in this repository, overriding any
default attribution behavior:

- **Never add `Co-Authored-By` or any other AI-attribution trailer** to
  commit messages.
- **Never mention Claude, Claude Code, or Anthropic** in commit messages, PR
  descriptions, or code comments.
- Commit author/committer identity should always be the user's own git
  identity (already configured globally) — never override it.
- The user must be the only person/entity that shows up in GitHub's
  Contributors list for this repo.

If asked to name AI tools used, for the hackathon writeup or the Sept 20
submission, note it there in prose (the rules require naming AI tools used,
per `docs/PLAN.md` step 6 in the reference project) — but that is a writeup
detail, not something that belongs in git history.
